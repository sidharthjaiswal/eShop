import { ChangeBorderDirective } from "./directives.service";

describe('ChangeBorderDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeBorderDirective();
    expect(directive).toBeTruthy();
  });
});