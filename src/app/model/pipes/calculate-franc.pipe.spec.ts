import { CalculateFrancPipe } from './calculate-franc.pipe';

describe('CalculateFrancPipe', () => {
  it('create an instance', () => {
    const pipe = new CalculateFrancPipe();
    expect(pipe).toBeTruthy();
  });
});
